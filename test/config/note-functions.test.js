//TODO: Mock the database so we can test this, or classify and inject the DB
describe('getAllNotes', () => {

    it('should return notes if present', () => {
    });

    it('should return empty list if none present', () => {

    });

    it('should throw if DB throws', () => {

    });
});